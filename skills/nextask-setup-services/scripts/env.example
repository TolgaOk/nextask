DB_PASSWORD=
VPS_HOST=

# nextask connection (derived from above)
NEXTASK_DB_URL="postgres://nextask:${DB_PASSWORD}@${VPS_HOST}:5432/nextask"
NEXTASK_SOURCE_REMOTE="http://nextask:${DB_PASSWORD}@${VPS_HOST}:3000/nextask/source.git"
